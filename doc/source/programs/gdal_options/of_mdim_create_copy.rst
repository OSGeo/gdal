
.. option:: -f, --of, --format, --output-format <OUTPUT-FORMAT>

    Which output multidimensional format to use. Allowed values are currently
    ``VRT`` (with restrictions), ``netCDF`` and ``Zarr``.
