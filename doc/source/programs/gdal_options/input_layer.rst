.. option:: --input-layer <INPUT-LAYER>

    Specifies the name of one or more layers to process. By default, all layers will be processed. To read and write all layers but only
    process a subset, use :option:`gdal_options_active_layer --active-layer`.

