usage()
{
	cat <<EOF
Usage: gdal-config [OPTIONS]
Options:
	[--prefix[=DIR]]
	[--libs]
	[--cflags]
EOF
	exit $1
}

if test $# -eq 0; then
	usage 1 1>&2
fi

case $1 in 
  --libs)
    echo $CONFIG_LIBS
    ;;

  --cflags)
    echo $CONFIG_CFLAGS
    ;;

  --prefix)
    echo $CONFIG_PREFIX
    ;;

  *)
    usage 1 1>&2
    ;;

esac
