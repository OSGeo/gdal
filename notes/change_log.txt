possible changes -

for adding ogrtriangle to the compiled list
    ogr/file.lst - add ogrtriangle.o

for adding SFCGAL support
    packages - mpfr, gimp, cgal4.3 (at least) - write script
    changes in SFCGAL dir -
        changes in the flag.make script
        check for static libs
    Vagrant changes - increase RAM size 

possible script -

    # Install some development libraries
    sudo apt-get install libgmp-dev
    sudo apt-get install libmpfr-dev
    sudo apt-get install libboost-all-dev

    # Download and extract the source code
    mkdir -p ~/src
    cd ~/src
    wget https://gforge.inria.fr/frs/download.php/file/34400/CGAL-4.5.1.tar.gz
    tar xf CGAL-4.5.1.tar.gz

    # Build
    cd CGAL-4.5.1
    cmake . && make && sudo make install

    # Clean up
    cd ../
    rm -rf CGAL-4.5.1 CGAL-4.5.1.tar.gz
